El servidor debe aceptar mensajes TCP en un puerto determinado 5050
El mensaje solamente es una cadena de texto que solo incluya los caracteres del alfabeto
El servidor contara cuantas veces aparece el ultimo carácter en la cadena de texto
Para probar el servidor usaremos JMeter
